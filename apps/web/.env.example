# Frontend environment variables

# API URL (proxied via next.config.js rewrites — usually not needed)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Error Tracking (INFRA-004) — install @sentry/nextjs to enable
NEXT_PUBLIC_SENTRY_DSN=
